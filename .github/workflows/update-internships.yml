name: Update Internship Listings

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run scraper
      env:
        GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        SHEET_ID: ${{ secrets.SHEET_ID }}
      run: |
        python scraper.py
```

4. Scroll down and click **"Commit changes"**

---

### Part D: Verify Your File Structure

After creating all files, click on your repository name at the top to go back to the main page. You should see:
```
📁 .github/
  📁 workflows/
    📄 update-internships.yml
📄 README.md
📄 requirements.txt
📄 scraper.py
